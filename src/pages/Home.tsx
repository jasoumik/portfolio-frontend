import { Container, Typography } from "@mui/material";

const Home = () => {
    return (
        <Container>
            <Typography variant="h3" gutterBottom>
                Welcome to My Portfolio
            </Typography>
            <Typography variant="body1">
                Hi, I'm a developer passionate about building great software.
            </Typography>
        </Container>
    );
};

export default Home;
